{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "rimraf dist && observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable",
    "test": "mocha -b 'tests/*.test.js'",
    "test_all": "mocha -b 'tests/*.test.js'",
    "intg": "mocha -b 'tests/*half*.test.js'"
  },
  "dependencies": {
    "@observablehq/framework": "^1.12.0",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0",
    "mocha": "^10.7.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mocha": "^10.1.0",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
